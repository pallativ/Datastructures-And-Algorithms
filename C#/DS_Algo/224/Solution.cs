using System;
using System.Collections.Generic;
using System.Text;

namespace Datastructures._224
{
    public class Solution
    {
        //private Stack<String> stack = new Stack<string>();
        //public int Calculate(string s)
        //{
        //    for (int i = 0; i < s.Length; i++)
        //    {
        //        if (s[i] == '(' || s[i] == '+' || s[i] == '-')
        //            stack.Push(s[i].ToString());
        //        else if (char.IsDigit(s[i]) && int.TryParse(stack.Peek(), out int val))
        //        {
        //            stack.Pop();
        //            var value = (val * 10 + int.Parse(s[i].ToString())).ToString();
        //            stack.Push(value);
        //        }
        //        else if (char.IsDigit(s[i]) && !int.TryParse(stack.Peek(), out int val1))
        //        {
        //            stack.Push(s[i].ToString());
        //        }
        //    }
        //}
    }
}
